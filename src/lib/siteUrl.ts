/** URL de produção do app — usada nos links que o Supabase manda por
 * e-mail (confirmação de cadastro, redefinição de senha) e que precisam
 * apontar pra cá, nunca pro localhost do ambiente de desenvolvimento.
 * Atualizar aqui se o domínio mudar algum dia (e também em Supabase →
 * Authentication → URL Configuration → Site URL / Redirect URLs). */
export const SITE_URL = "https://missao-casa-theta.vercel.app";
